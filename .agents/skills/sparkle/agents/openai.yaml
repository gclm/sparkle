interface:
  display_name: "Sparkle"
  short_description: "Repo-specific patterns and workflows for sparkle"
  default_prompt: "Use the sparkle repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true